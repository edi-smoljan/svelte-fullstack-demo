datasource db {
  url      = env("DATABASE_URL")
  provider = "mysql"
}

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native"]
}

model Todo {
  uid       String      @id @default(cuid())
  created_at DateTime
  text String
  done Boolean
}